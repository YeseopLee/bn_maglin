# 씬 정리 및 매니저 생명주기 가이드

## 현재 상황
"Some objects were not cleaned up when closing the scene" 경고가 발생하지만, 이는 **정상적인 동작**입니다.

### DontDestroyOnLoad로 유지되는 핵심 매니저들:
- **PlayerManager**: 플레이어 상태 (체력, 마나, 골드 등)
- **SaveManager**: 게임 세이브/로드
- **FieldManager**: 필드 효과 관리
- **CardManager**: 카드 시스템
- **AudioManager**: 오디오 관리
- **RelicManager**: 유물 시스템

이들은 게임 전체에서 지속적으로 사용되므로 씬 전환 시에도 유지되어야 합니다.

## 경고 메시지 해결 방법 (선택사항)

### 1. 씬별 매니저 정리
타이틀 화면이나 게임 종료 시에만 매니저들을 정리:

```csharp
public class SceneCleanupManager : MonoBehaviour
{
    public static void CleanupOnGameEnd()
    {
        // 전투 관련 매니저만 정리 (핵심 매니저는 유지)
        if (TargetManager.Instance != null)
            Destroy(TargetManager.Instance.gameObject);
            
        if (MonsterSpawnManager.Instance != null)
            Destroy(MonsterSpawnManager.Instance.gameObject);
    }
}
```

### 2. 매니저 유효성 검사 강화
각 매니저의 `OnDestroy`에서 다른 오브젝트를 생성하지 않도록 보장:

```csharp
private void OnDestroy()
{
    // OnDestroy에서는 정리만 수행, 새로운 오브젝트 생성 금지
    if (_instance == this)
    {
        _instance = null;
        // 이벤트 구독 해제 등만 수행
    }
}
```

## 권장사항
현재 경고는 **무시해도 됩니다**. 게임의 정상적인 동작을 방해하지 않으며, 핵심 매니저들의 의도된 동작입니다.

게임 성능이나 동작에 실제 문제가 있는 경우에만 추가적인 정리 로직을 구현하는 것을 권장합니다.
