## TargetType 확장 및 전투 로직 적용 요약

### 변경 파일
- `Assets/Scripts/Cards/CardSO.cs`
- `Assets/Scripts/Battle/TargetManager.cs`
- `Assets/Scripts/Battle/BattleTestController.cs`

### 주요 변경점
1) `TargetType` 확장 (직렬화 호환을 위해 기존 값 뒤에만 추가)
   - `ChainFrontHits`: 가장 앞 몬스터부터 뒤로 targetCount회 연속 타격 (앞이 죽으면 남은 횟수는 다음 몬스터로)
   - `PullFrontmostForward`: 가장 앞 몬스터를 플레이어 쪽으로 targetCount칸 강제 전진
   - `PlayerFrontLine`: 플레이어 앞 targetCount칸 세로 라인 범위 공격 (플레이어 X, +Y 범위)
   - `TargetFrontStrip`: 타겟 포함 플레이어쪽으로 targetCount칸 세로 스트립 공격 (타겟 마커 기반)
   - `TargetBackStrip`: 타겟 포함 플레이어 반대쪽으로 targetCount칸 세로 스트립 공격 (타겟 마커 기반)

2) `TargetManager`에 유틸리티 API 추가
   - `DamageFrontN(count, damage)`: 앞에서 N명 피해
   - `DamageBackN(count, damage)`: 뒤에서 N명 피해
   - `ChainHitsFromFront(hits, damage)`: 앞 기준 체인 히트
   - `PullFrontmostForward(tiles)`: 앞의 적 강제 전진
   - `DamagePlayerFrontLine(range, damage)`: 플레이어 앞 라인 범위 피해
   - `DamageTargetFrontStrip(target, range, damage)`: 타겟 포함 앞 스트립
   - `DamageTargetBackStrip(target, range, damage)`: 타겟 포함 뒤 스트립

3) `BattleTestController`에서 새 TargetType에 따른 처리 연결
   - VFX 미사용: `ApplyCardEffectWithoutVFX`에서 스위치 문으로 처리
   - VFX 사용: `ApplyVFXDamageToTargets`에서 새 타입에 맞게 `TargetManager` API 호출
   - `AllIncludingSelf`는 적 전체 + 플레이어 동시 피해 지원

### 사용/설정 가이드
- `CardSO`에서 `Target`(TargetType)과 `TargetCount`(N/횟수/칸 수)를 설정하세요.
- 라인/스트립 공격은 그리드 기준:
  - 플레이어 기준 앞: 동일 X, Y가 플레이어보다 큰 방향(+Y)
  - 스트립: 타겟의 X를 기준으로 Y 범위 지정

### 주의사항
- 기존 TargetType의 순서는 유지되고, 새 항목은 뒤에만 추가하여 에셋 직렬화 호환성을 보장했습니다.
- 강제 전진은 충돌/점유 검증 없이 위치만 변경합니다. 필요 시 차후 충돌 처리 로직을 보강하세요.


