# 플레이어 객체 배치 가이드

## 개요
이제 PlayerBattleManager에서 자동으로 플레이어를 생성하지 않으므로, 씬에 직접 플레이어 오브젝트를 배치해야 합니다. 이 가이드는 **TestBattleScene**에서 플레이어 객체를 올바르게 설정하는 방법을 설명합니다.

## 1. 플레이어 객체 생성

### 방법 1: 새로운 GameObject 생성
1. **Hierarchy**에서 우클릭 → **Create Empty**
2. 이름을 **"Player"**로 변경
3. 다음 컴포넌트들을 추가:

#### 필수 컴포넌트:
- **SpriteRenderer**: 플레이어 스프라이트 표시
- **Rigidbody2D**: 물리 시스템 (타일맵과 상호작용)
- **BoxCollider2D**: 충돌 감지 (바닥과 충돌하기 위해)

#### SpriteRenderer 설정:
- **Sprite**: 임시 스프라이트 할당 (시작 시 PlayerManager 스프라이트로 자동 교체됨)
- **Sorting Layer**: "Player" 또는 "Default"
- **Order in Layer**: 10 (몬스터보다 앞에 표시)
- **중요**: 할당한 스프라이트는 게임 시작 시 PlayerManager의 애니메이션 스프라이트로 자동 교체됩니다

#### Rigidbody2D 설정:
- **Body Type**: Dynamic (물리 시뮬레이션 활성화 - 필수!)
- **Mass**: 1
- **Linear Drag**: 0
- **Angular Drag**: 0.05
- **Gravity Scale**: 1 (바닥으로 떨어지도록 - 중요!)
- **Freeze Rotation Z**: 체크 (회전 방지)
- **중요**: 입장 애니메이션은 물리 시뮬레이션을 유지하면서 실행됩니다

#### BoxCollider2D 설정:
- **Is Trigger**: 체크 해제 (물리 충돌 필요)
- **Size**: Vector2(1, 1) 또는 스프라이트 크기에 맞게 조정

### 방법 2: 기존 프리팹 사용
1. **Project** 창에서 기존 플레이어 프리팹을 찾기
2. **Hierarchy**로 드래그하여 배치
3. 위의 컴포넌트 설정을 확인 및 조정

## 2. 플레이어 위치 설정

### Grid 기반 위치 설정:
- **Transform Position**: 타일맵의 적절한 위치에 배치
- 일반적으로 **X = 0, Y = 0** (그리드의 중앙)
- **Z = 0** (2D 게임이므로)

### 권장 위치:
- 그리드의 왼쪽 끝 (X = 0~2)에 배치하여 입장 애니메이션이 자연스럽게 보이도록 설정

## 3. PlayerBattleManager 설정

### Inspector 설정:
1. **PlayerBattleManager** 오브젝트 선택
2. **Player Prefab** 필드에 생성한 플레이어 오브젝트 할당

### 자동 감지 설정 (Inspector 설정을 원하지 않는 경우):
다음 중 하나를 수행:
1. 플레이어 오브젝트에 **"Player"** 태그 설정
2. 플레이어 오브젝트 이름을 **"Player"**로 설정

## 4. 타일맵과의 연동

### Ground Tilemap 확인:
1. **TestBattleScene**의 **Grid** → **Ground** 확인
2. 타일이 올바르게 배치되어 있는지 확인
3. **Tilemap Collider2D** 컴포넌트가 있는지 확인

### 플레이어 물리 설정:
- **Rigidbody2D**의 **Gravity Scale**을 1로 설정하여 바닥으로 떨어지도록 함
- **BoxCollider2D**의 **Is Trigger**를 해제하여 타일맵과 충돌하도록 함

## 5. 테스트 및 확인

### 실행 테스트:
1. **Play** 버튼을 눌러 테스트
2. 플레이어가 올바른 위치에 나타나는지 확인
3. 입장 애니메이션이 타일을 기반으로 실행되는지 확인

### 디버그 로그 확인:
- **Console** 창에서 다음과 같은 로그 확인:
  ```
  [PlayerBattleManager] Inspector에서 할당된 플레이어 사용: Player
  [PlayerBattleManager] 타일 기반 애니메이션: (0,0) → (2,0)
  ```

## 6. 문제 해결

### 플레이어가 보이지 않는 경우:
1. **PlayerBattleManager**의 **Player Prefab** 필드 확인
2. 플레이어 오브젝트의 **태그** 또는 **이름** 확인
3. **SpriteRenderer**의 **Sprite** 할당 확인

### 플레이어가 떨어지는 경우:
1. **Ground Tilemap**의 **Tilemap Collider2D** 확인
2. 플레이어의 **Rigidbody2D** → **Gravity Scale** 확인
3. 플레이어의 **BoxCollider2D** → **Is Trigger** 해제 확인

### 입장 애니메이션이 작동하지 않는 경우:
1. **GridFieldManager**가 초기화되었는지 확인
2. 플레이어 위치가 그리드 범위 내에 있는지 확인
3. 디버그 모드를 켜서 로그 확인

## 7. 고급 설정

### 커스텀 플레이어 스프라이트:
- **Player Manager**에서 설정한 스프라이트가 자동으로 적용됨
- 필요시 **SpriteRenderer**에서 직접 스프라이트 변경 가능

### 애니메이션 설정:
- **Player Manager**의 애니메이션 시스템과 자동 연동
- Walk, Idle 애니메이션이 자동으로 적용됨

### 위치 정밀 조정:
- **Transform** 위치를 조정하여 원하는 위치에 정확히 배치
- **GridFieldManager**가 자동으로 그리드 위치로 스냅함

## 8. 예제 설정

### 완성된 플레이어 오브젝트 예시:
```
Player (GameObject)
├── Transform: Position (2, 0, 0)
├── SpriteRenderer: Sprite = PlayerSprite, Order in Layer = 10
├── Rigidbody2D: Dynamic, Mass = 1, Gravity Scale = 1
└── BoxCollider2D: Size = (1, 1), Is Trigger = false
```

### PlayerBattleManager 설정 예시:
```
PlayerBattleManager (Component)
├── Player Prefab: Player (GameObject 참조)
├── Debug Mode: true (테스트 시)
└── Entrance Animation Duration: 2.0
```

이 가이드를 따라 설정하면 플레이어가 타일맵 기반으로 자연스럽게 동작하며, 2D 횡스크롤 게임다운 움직임을 보여줄 것입니다.
