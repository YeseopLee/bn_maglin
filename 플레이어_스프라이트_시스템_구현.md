# 플레이어 스프라이트 시스템 구현 완료

## 개요
전투화면에서 파란색 네모박스로 표현되던 플레이어를 실제 스프라이트 시스템으로 교체하여 다양한 애니메이션 상태를 지원하도록 구현했습니다.

## 주요 기능

### 1. 플레이어 애니메이션 상태
- **대기 모션 (Idle)**: 평상시 상태
- **공격 모션 1-3 (Attack1-3)**: 카드 사용에 따른 다양한 공격 애니메이션 (현재는 공격1만 사용)
- **피격 모션 (Hit)**: 몬스터 공격 시 표현
- **사망 모션 (Death)**: 플레이어 사망 시 표현

### 2. 자동 애니메이션 시스템
- **카드 사용 시**: 자동으로 공격 애니메이션 실행 후 대기 상태로 복귀
- **피격 시**: 자동으로 피격 애니메이션 실행 후 대기 상태로 복귀
- **사망 시**: 사망 애니메이션 실행 및 유지

## 수정된 파일

### PlayerManager.cs
#### 추가된 기능
1. **스프라이트 필드 추가**
   ```csharp
   [Header("플레이어 스프라이트")]
   [SerializeField] private Sprite idleSprite;
   [SerializeField] private Sprite attackSprite1;
   [SerializeField] private Sprite attackSprite2;
   [SerializeField] private Sprite attackSprite3;
   [SerializeField] private Sprite hitSprite;
   [SerializeField] private Sprite deathSprite;
   ```

2. **애니메이션 상태 열거형**
   ```csharp
   public enum PlayerAnimationState
   {
       Idle, Attack1, Attack2, Attack3, Hit, Death
   }
   ```

3. **이벤트 시스템**
   ```csharp
   public static event Action<PlayerAnimationState> OnPlayerAnimationChanged;
   ```

4. **애니메이션 관리 메서드들**
   - `SetAnimationState()`: 애니메이션 상태 변경
   - `GetCurrentSprite()`: 현재 상태에 맞는 스프라이트 반환
   - `PlayAttackAnimation()`: 공격 애니메이션 실행
   - `PlayHitAnimation()`: 피격 애니메이션 실행
   - `PlayDeathAnimation()`: 사망 애니메이션 실행
   - `ReturnToIdle()`: 대기 상태로 복귀

5. **자동 스프라이트 로드/생성 시스템**
   - Resources 폴더에서 스프라이트 자동 로드
   - 스프라이트가 없으면 기본 컬러 스프라이트 자동 생성
   - 각 상태별로 다른 색상 적용 (대기: 파랑, 공격1: 빨강, 피격: 마젠타 등)

#### 수정된 기능
1. **TakeDamage()**: 피격 시 자동으로 피격 애니메이션 실행
2. **HandlePlayerDeath()**: 사망 시 자동으로 사망 애니메이션 실행
3. **GetPlayerSprite()**: 현재 상태에 맞는 스프라이트 반환하도록 수정
4. **ResetToDefault()**: 리셋 시 대기 상태로 설정
5. **OnBattleStart()**: 전투 시작 시 대기 상태로 초기화

### BattleTestController.cs
#### 추가된 기능
1. **PlayerManager 애니메이션 이벤트 구독**
   ```csharp
   PlayerManager.OnPlayerAnimationChanged += OnPlayerAnimationChanged;
   ```

2. **애니메이션 변경 이벤트 핸들러**
   ```csharp
   private void OnPlayerAnimationChanged(PlayerManager.PlayerAnimationState newState)
   {
       if (PlayerBattleManager.Instance != null)
       {
           PlayerBattleManager.Instance.UpdatePlayerSprite();
       }
   }
   ```

#### 수정된 기능
1. **ExecuteCardEffect()**: 카드 사용 시 자동으로 공격 애니메이션 실행
   - 데미지나 힐이 있는 카드 사용 시 공격1 애니메이션 실행
   - 0.5초 후 자동으로 대기 상태로 복귀

## 사용 방법

### 1. 스프라이트 설정
1. `Resources/Player/` 폴더에 다음 스프라이트들을 배치:
   - `PlayerIdle.png`: 대기 모션
   - `PlayerAttack1.png`: 공격 모션 1
   - `PlayerAttack2.png`: 공격 모션 2 (선택사항)
   - `PlayerAttack3.png`: 공격 모션 3 (선택사항)
   - `PlayerHit.png`: 피격 모션 (선택사항)
   - `PlayerDeath.png`: 사망 모션 (선택사항)

2. 스프라이트가 없으면 자동으로 기본 컬러 스프라이트가 생성됩니다.

### 2. 인스펙터에서 설정
PlayerManager 인스펙터에서 "플레이어 스프라이트" 섹션에서 각 상태의 스프라이트를 직접 할당할 수도 있습니다.

### 3. 코드에서 애니메이션 제어
```csharp
// 수동으로 애니메이션 실행
PlayerManager.Instance.PlayAttackAnimation(1, 0.5f);  // 공격1 애니메이션, 0.5초 후 복귀
PlayerManager.Instance.PlayHitAnimation(0.3f);        // 피격 애니메이션, 0.3초 후 복귀
PlayerManager.Instance.SetAnimationState(PlayerManager.PlayerAnimationState.Idle);  // 즉시 대기 상태로

// 현재 스프라이트 가져오기
Sprite currentSprite = PlayerManager.Instance.GetCurrentSprite();
```

## 기술적 세부사항

### 애니메이션 타이밍
- **공격 애니메이션**: 0.5초 후 자동으로 대기 상태로 복귀
- **피격 애니메이션**: 0.3초 후 자동으로 대기 상태로 복귀
- **사망 애니메이션**: 복귀하지 않고 유지

### 이벤트 기반 업데이트
- PlayerManager에서 애니메이션 상태가 변경되면 자동으로 이벤트 발생
- BattleTestController가 이벤트를 받아 PlayerBattleManager에 스프라이트 업데이트 요청
- UI와 게임 로직의 분리를 통한 모듈화된 구조

### 기본 스프라이트 시스템
스프라이트가 설정되지 않은 경우 자동으로 생성되는 기본 스프라이트:
- **대기**: 파란색 (Color.blue)
- **공격1**: 빨간색 (Color.red)
- **공격2**: 노란색 (Color.yellow)
- **공격3**: 초록색 (Color.green)
- **피격**: 마젠타 (Color.magenta)
- **사망**: 검은색 (Color.black)

## 향후 확장 가능성

1. **추가 공격 애니메이션**: 카드 타입에 따라 공격2, 공격3 사용 가능
2. **애니메이션 지속시간 커스터마이징**: 카드별로 다른 애니메이션 지속시간 설정
3. **복합 애니메이션**: 여러 상태를 조합한 복잡한 애니메이션 시퀀스
4. **사운드 효과**: 애니메이션 상태 변경 시 효과음 재생
5. **파티클 효과**: 애니메이션과 동기화된 시각 효과

## 주의사항

1. **PlayerBattleManager 의존성**: `PlayerBattleManager.Instance.UpdatePlayerSprite()` 메서드가 필요합니다.
2. **성능 고려**: 스프라이트 생성은 초기화 시에만 수행되므로 런타임 성능에 영향 없음
3. **메모리 관리**: 동적으로 생성된 텍스처는 씬 전환 시 자동으로 정리됨

이제 플레이어가 다양한 상황에 맞는 시각적 피드백을 제공하여 더욱 생동감 있는 전투 경험을 제공할 수 있습니다.
