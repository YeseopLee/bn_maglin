# 카드 드로우 타이밍 및 컨트롤러 통합 최종 수정사항

## 📋 수정 개요

메인화면에서 전투화면으로 넘어갈 때 발생했던 다음 문제들을 해결했습니다:
1. **카드 드로우 타이밍 문제**: 로딩 화면이 끝나기 전에 카드 드로우가 완료되어 애니메이션을 볼 수 없는 문제
2. **컨트롤러 중복 문제**: BattleController와 BattleTestController 중복으로 인한 혼란
3. **프로덕션 연결 문제**: FloorManager → BattleTestController 간 데이터 흐름 끊김
4. **전투 초기화 문제**: 프로덕션 모드에서 몬스터 생성 및 카드 시스템 미작동

---

## 🔧 주요 수정사항

### 1. **카드 드로우 타이밍 최적화**

#### 1.1 CardManager.cs 수정
- **변경점**: `OnBattleStarted`에서 즉시 드로우 제거, `PerformInitialCardDraw` 메서드 추가
- **효과**: 몬스터 스폰 애니메이션 완료 후 카드 드로우 실행

```csharp
// 기존: 전투 시작 즉시 드로우
private void OnBattleStarted(BattleSO battleData)
{
    // 초기 드로우 (Card 인스턴스 기반)
    DrawCards(MaxHandSize, true);  // ❌ 즉시 실행
}

// 수정: 별도 메서드로 분리
public void PerformInitialCardDraw()
{
    DrawCards(MaxHandSize, true);  // ✅ 몬스터 스폰 후 호출
    hasPerformedInitialDraw = true;
}
```

#### 1.2 BattleInitializationSequence.cs 수정
- **변경점**: 몬스터 스폰 애니메이션 완료 후 `PerformInitialCardDraw` 호출
- **효과**: 카드 드로우 애니메이션을 플레이어가 볼 수 있음

```csharp
// 몬스터 스폰 → 카드 드로우 순서 보장
await StartMonsterSpawnAnimations();
await PerformInitialCardDraw();  // ✅ 순서 조정
```

#### 1.3 턴 시작 시 중복 드로우 방지
- **변경점**: `hasPerformedInitialDraw` 플래그로 첫 턴 카드 드로우 방지
- **효과**: 초기 드로우와 턴 시작 드로우가 중복되지 않음

---

### 2. **컨트롤러 통합 (BattleController → BattleTestController)**

#### 2.1 BattleTestController.cs 기능 확장
- **테스트 모드**: `useTestMode = true`일 때 기존 테스트 기능 유지
- **프로덕션 모드**: `useTestMode = false`일 때 FloorManager 연동

```csharp
[Header("Battle Data")]
[SerializeField] private BattleStageSO currentBattleStage;

[Header("Test Data (테스트 모드용)")]
[SerializeField] private List<CardSO> testCards = new List<CardSO>();
[SerializeField] private BattleStageSO testBattleStage;
[SerializeField] private bool useTestMode = false; // ✅ 모드 전환 가능
```

#### 2.2 FloorManager 이벤트 연동 추가
- **추가된 기능**: FloorManager의 층 시작/완료 이벤트 구독
- **효과**: 층별 전투 자동 시작 및 보상 처리

```csharp
// FloorManager 이벤트 구독
private void SubscribeToFloorManagerEvents()
{
    if (FloorManager.Instance != null)
    {
        FloorManager.Instance.OnFloorStarted += OnFloorStarted;    // ✅
        FloorManager.Instance.OnFloorCompleted += OnFloorCompleted; // ✅
    }
}
```

#### 2.3 BattleController.cs 삭제
- **변경점**: 중복된 BattleController 클래스 완전 제거
- **참조 수정**: 모든 BattleController 참조를 BattleTestController로 변경

---

### 3. **프로덕션 모드 연결 복구**

#### 3.1 배틀 스테이지 로딩 시스템 개선
```csharp
private void LoadBattleStageForFloor(int floor, FloorType floorType)
{
    // 1. FloorManager에서 현재 배틀 스테이지 확인
    var battleStage = FloorManager.Instance.GetCurrentBattleStage();
    
    // 2. 없으면 층에 맞는 스테이지 요청
    if (battleStage == null)
        battleStage = FloorManager.Instance.GetBattleStageForFloor(floor, floorType);
    
    // 3. 여전히 없으면 리소스에서 로드
    if (battleStage == null)
        battleStage = Resources.Load<BattleStageSO>($"BattleStage/F{floor:D2}");
    
    // 4. 모두 실패하면 기본 스테이지 생성
    if (battleStage == null)
        CreateDefaultBattleStage(floor, floorType);
}
```

#### 3.2 프로덕션 모드 전용 초기화 추가
```csharp
private void InitializeProductionMode()
{
    // 카드 시스템 확실히 초기화
    CardManager.Instance.InitializeDeckForNewGame();
    CardManager.Instance.InitializeCardInstanceSystem();
    
    // 플레이어 상태 초기화
    PlayerManager.Instance.OnBattleStart();
}
```

#### 3.3 FloorManager 연결 확인
- **변경점**: FloorManager → BattleTestController 데이터 흐름 복구
- **효과**: 메인화면에서 설정된 매니저들이 전투화면에서 정상 작동

---

### 4. **몬스터 스폰 시스템 개선**

#### 4.1 스폰 로직 통합
```csharp
private IEnumerator SpawnMonstersAndSetTarget()
{
    // 테스트/프로덕션 모드에 따라 스테이지 선택
    BattleStageSO stageToUse = useTestMode ? testBattleStage : currentBattleStage;
    
    if (stageToUse != null)
        MonsterSpawnManager.Instance?.SpawnTestMonsters(stageToUse); // ✅
    else
        MonsterSpawnManager.Instance?.SpawnTestMonsters(null); // 기본 몬스터
}
```

#### 4.2 애니메이션 순서 보장
- **순서**: 몬스터 스폰 애니메이션 → 카드 드로우 애니메이션
- **효과**: 플레이어가 모든 애니메이션을 순서대로 볼 수 있음

---

## 🔄 변경된 파일 목록

### 수정된 파일들
1. **Assets/Scripts/Cards/CardManager.cs**
   - 초기 카드 드로우 타이밍 조정
   - 중복 드로우 방지 로직 추가

2. **Assets/Scripts/Battle/BattleTestController.cs**
   - 프로덕션 모드 기능 추가
   - FloorManager 연동 기능 추가
   - 배틀 스테이지 로딩 시스템 개선

3. **Assets/Scripts/Battle/BattleInitializationSequence.cs**
   - BattleController → BattleTestController 참조 변경
   - 카드 드로우 타이밍 조정

4. **Assets/Scripts/Core/FloorManager.cs**
   - 주석 업데이트 (BattleController → BattleTestController)

### 삭제된 파일들
1. **Assets/Scripts/Battle/BattleController.cs** ❌ (완전 삭제)

---

## 🧪 테스트 방법

### 1. 테스트 모드 확인
```csharp
// BattleTestController에서
useTestMode = true;  // 기존 테스트 기능 작동 확인
```

### 2. 프로덕션 모드 확인
```csharp
// BattleTestController에서
useTestMode = false; // FloorManager 연동 확인
```

### 3. 애니메이션 순서 확인
1. 메인화면 → 전투화면 이동
2. 로딩 화면 → 몬스터 스폰 애니메이션 → 카드 드로우 애니메이션 순서 확인

---

## ✅ 해결된 문제들

| 문제 | 해결 방법 | 결과 |
|------|-----------|------|
| 카드 드로우 애니메이션이 보이지 않음 | 몬스터 스폰 후 카드 드로우 실행 | ✅ 애니메이션 순서 보장 |
| 프로덕션 모드에서 전투 미작동 | FloorManager 연동 복구 | ✅ 정상적인 전투 진행 |
| 몬스터가 생성되지 않음 | currentBattleStage 연결 수정 | ✅ 몬스터 정상 스폰 |
| 카드가 없음 | 프로덕션 모드 카드 초기화 추가 | ✅ 카드 시스템 정상 작동 |
| 컨트롤러 중복 | BattleController 삭제 | ✅ 코드 정리 완료 |

---

## 🎯 결과

메인화면에서 전투화면으로 넘어갈 때:
1. ✅ **로딩 화면 정상 표시**
2. ✅ **몬스터 스폰 애니메이션 재생**  
3. ✅ **카드 드로우 애니메이션 재생**
4. ✅ **전투 시스템 정상 작동**
5. ✅ **FloorManager 연동 완료**

모든 시스템이 올바른 순서로 실행되어 플레이어가 완전한 전투 경험을 할 수 있게 되었습니다.
