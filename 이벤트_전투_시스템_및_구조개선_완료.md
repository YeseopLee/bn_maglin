# 이벤트 전투 시스템 및 구조 개선 완료 보고서

## 📋 개요
이벤트에서 특정 전투를 발생시키는 기능과 이벤트 관리 구조 개선을 완료했습니다.

## 🎯 주요 구현 사항

### 1. 이벤트 전투 시스템 추가

#### 1.1 EventSO.cs 확장
- **Battle 타입 보상 추가**: `EventRewardType.Battle` 열거형 추가
- **BattleSO 필드 추가**: `EventReward` 클래스에 `battleData` 필드 추가
- **네임스페이스 추가**: `using Maglin.Battle;` 추가

```csharp
public enum EventRewardType
{
    // ... 기존 타입들
    Battle          // 전투 발생 (새로 추가)
}

[System.Serializable]
public class EventReward
{
    // ... 기존 필드들
    public BattleSO battleData;         // 전투 타입인 경우 (새로 추가)
}
```

#### 1.2 EventManager.cs 전투 처리 로직
- **대기 전투 시스템**: `pendingBattleData` 변수로 전투를 즉시 시작하지 않고 대기
- **전투 시작 타이밍 조정**: 이벤트 결과 확인 후 전투 시작
- **이벤트 전투 플로우**:
  1. 선택지 선택 → 보상 적용 → 전투 대기 상태
  2. "계속" 버튼 클릭 → `EndEvent()` 호출 → 전투 시작

```csharp
case EventRewardType.Battle:
    if (reward.battleData != null && !isPenalty)
    {
        // 전투를 즉시 시작하지 않고 대기 상태로 설정
        pendingBattleData = reward.battleData;
        Debug.Log($"Event battle queued: {reward.battleData.BattleName}");
    }
    break;
```

#### 1.3 FloorManager.cs 이벤트 전투 모드 지원
- **이벤트 전투 모드 변수**: `isEventBattleMode`, `savedEventForBattle`
- **이벤트 전투 완료 처리**: 일반 층 진행 대신 원래 이벤트로 복귀
- **전투 설정 메서드**: `SetEventBattleMode()`, `CompleteEventBattle()`

```csharp
// 이벤트 전투 모드 관련
private bool isEventBattleMode = false;
private EventSO savedEventForBattle = null;

public void SetEventBattleMode(bool isEventBattle, EventSO savedEvent = null)
{
    isEventBattleMode = isEventBattle;
    savedEventForBattle = savedEvent;
}
```

#### 1.4 BattleTestController.cs 이벤트 전투 처리
- **이벤트 전투 감지**: `HandleEventBattle()` 메서드로 이벤트 전투 모드 처리
- **카드 덱 초기화**: 이벤트 전투에서도 스타터 덱으로 초기화되도록 수정
- **임시 BattleStage 생성**: 이벤트 전투용 임시 BattleStage 생성

```csharp
private void HandleEventBattle()
{
    // ... 이벤트 전투 데이터 가져오기
    var tempStage = CreateEventBattleStage(battleData);
    currentBattleStage = tempStage;

    // 매니저들 초기화 (카드 덱 포함)
    InitializeManagers();
    InitializeProductionMode(); // 스타터 덱으로 초기화

    // 전투 시작
    StartBattle();
}
```

### 2. 이벤트 관리 구조 개선

#### 2.1 중복 제거 및 단일 책임 분리
- **FloorManager**: 층 진행 관리에만 집중, `availableEvents` 제거
- **EventManager**: 이벤트 시스템 전담, 모든 이벤트 관련 로직 집중
- **중복 메서드 제거**: FloorManager의 `SelectRandomEvent()` 제거

#### 2.2 EventManager 중심 구조
- **이벤트 선택 로직**: EventManager에서 일원화
- **고급 기능 활용**: LINQ 사용, OneTimeOnly 체크, 가중치 기반 선택
- **외부 접근 메서드**: `SelectEventForFloor()` 공개 메서드 추가

```csharp
// FloorManager에서 EventManager 사용
EventSO selectedEvent = EventManager.Instance.SelectEventForFloor(currentFloor);
```

### 3. EventTestController 개선

#### 3.1 실제 시스템 연동
- **테스트 이벤트 배열 제거**: `testEvents` 필드 삭제
- **실제 EventManager 설정 사용**: `EventManager.Instance.GetAvailableEvents()` 활용
- **실제 선택 로직 사용**: `SelectEventForFloor()` 메서드 사용

#### 3.2 개선된 디버그 기능
- **상세 이벤트 정보**: 가중치, 층 범위, 출현 가능 여부 표시
- **실시간 시스템 상태**: EventManager의 실제 설정 확인

```csharp
private void LogAvailableEvents()
{
    var availableEvents = EventManager.Instance.GetAvailableEvents();
    // ... 각 이벤트의 상세 정보 로그
    Debug.Log($"{i}: {eventSO.EventName} (층 {eventSO.MinFloor}-{eventSO.MaxFloor}, 가중치 {eventSO.SpawnWeight}) " +
             $"[{testFloor}층 출현 가능: {(canAppear ? "O" : "X")}]");
}
```

## 🔄 전투 플로우

### 이벤트 전투 전체 플로우
1. **이벤트 진입** → 선택지 선택
2. **보상 적용** → Battle 타입 보상 시 `pendingBattleData` 설정
3. **결과 확인** → "계속" 버튼 클릭
4. **전투 시작** → `EndEvent()` → `StartEventBattle()` → 전투 씬 로드
5. **전투 진행** → 카드는 스타터 덱으로 초기화됨
6. **전투 완료** → `CompleteEventBattle()` → 다음 층으로 진행

### 일반 전투와의 차이점
- **층수 증가 없음**: 이벤트 전투는 현재 층에서 발생
- **이벤트 연계**: 전투 후 원래 이벤트 맥락으로 복귀 가능
- **즉시 진행**: 전투 승리 시 다음 층으로 자동 진행

## 🛠️ 주요 코드 변경사항

### EventUIManager.cs 연동
- **대기 전투 감지**: `EventManager.Instance.HasPendingBattle` 체크
- **버튼 텍스트 변경**: 대기 전투 시 "전투 시작", 일반 시 "계속"
- **결과 텍스트 조정**: 전투 대기 시 안내 메시지 추가

```csharp
// 대기 중인 전투가 있으면 메시지 추가
if (EventManager.Instance != null && EventManager.Instance.HasPendingBattle)
{
    resultMessage += "\n\n전투가 기다리고 있습니다...";
}
```

## 🎮 사용법

### EventSO 설정
1. EventSO에서 선택지의 보상으로 `EventRewardType.Battle` 선택
2. `battleData` 필드에 원하는 BattleSO 할당
3. 보상/페널티 구분에 따라 설정 (페널티로는 전투 발생 안함)

### 테스트 방법
1. EventTestController를 씬에 배치
2. EventManager에 `availableEvents` 설정
3. "Start Random Event" 버튼으로 실제 시스템 테스트
4. 콘솔에서 이벤트 선택 과정 및 가중치 확인

## ✅ 검증 완료 사항

- [x] 이벤트에서 전투 발생 기능
- [x] 전투 후 층수 증가 (정상 진행)
- [x] 카드 덱 초기화 (스타터 덱으로)
- [x] 이벤트 결과 확인 후 전투 시작
- [x] EventManager 중심 구조 개선
- [x] 중복 코드 제거
- [x] 실제 시스템 연동 테스트

## 🔧 추후 개선 가능 사항

1. **이벤트 전투 보상**: 별도의 이벤트 전투 보상 시스템
2. **연속 전투**: 하나의 이벤트에서 여러 전투 발생
3. **조건부 전투**: 특정 조건에 따른 전투 발생
4. **전투 난이도 조절**: 이벤트 맥락에 맞는 전투 강도

---

**구현 완료일**: 2024년
**담당**: AI Assistant
**상태**: ✅ 완료
